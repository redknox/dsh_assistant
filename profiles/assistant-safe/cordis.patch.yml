# Recovery overlay on the governed assistant Profile. Ordinary patches cannot
# disable dsh-assistant or remove governance/registry authority.
- id: jobs
  disabled: true
- id: dsh-assistant
  config:
    safeMode: true
    jobs:
      autoTickMs: null
